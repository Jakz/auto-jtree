package com.jack.autotree.creators;

import com.jack.autotree.nodes.InnerNode;
import com.jack.autotree.proxies.ArrayProxy;

public class ArrayNodeCreator
{
  public void addElementAtIndex(ArrayProxy proxy)
  {
    InnerNode parent = proxy.getParentNode();
    
    
  }
}
